% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_overlap.R
\name{reviewOverlap}
\alias{reviewOverlap}
\title{Create a Co-occurrence Heatmap}
\usage{
reviewOverlap(
  data,
  col1,
  col2,
  sep = "\\r\\n",
  fill = "#7BB0D1",
  base_size = 12,
  na.rm = TRUE,
  na_label = "Not reported",
  studlabs = FALSE,
  study_id = StudyID,
  label_wrap = 15
)
}
\arguments{
\item{data}{A data frame with at least the columns named by \code{col1} and
\code{col2}.}

\item{col1}{First column (quoted or unquoted), mapped to the x-axis.}

\item{col2}{Second column (quoted or unquoted), mapped to the y-axis.}

\item{sep}{Character. Separator for multi-value cells. Defaults to \code{"\\r\\n"}.}

\item{fill}{Character. High-end color for the gradient. Defaults to
\code{"#7BB0D1"}.}

\item{base_size}{Numeric. Base font size in points. Defaults to \code{12}.}

\item{na.rm}{Logical. Drop missing values? Defaults to \code{TRUE}.}

\item{na_label}{Character. Label for missing values when \code{na.rm = FALSE}.
Defaults to \code{"Not reported"}.}

\item{studlabs}{Logical. If \code{TRUE}, show comma-separated study IDs inside
each tile instead of (or below) the count. Defaults to \code{FALSE}.}

\item{study_id}{Column containing study identifiers (quoted or unquoted).
Used when \code{studlabs = TRUE}. Defaults to \code{StudyID}.}

\item{label_wrap}{Integer. Wrap axis tick labels longer than this many
characters onto multiple lines. Set to \code{NULL} or \code{Inf} to disable.
Defaults to \code{15}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Counts how many studies share each combination of values in two columns
and displays the result as a tile heatmap. Returns a \link[ggplot2:ggplot]{ggplot2::ggplot}
object.
}
\examples{
df <- data.frame(
  StudyID = c("S1", "S2", "S3", "S4"),
  Design  = c("RCT", "Cohort", "RCT", "Case-control"),
  Country = c("UK", "UK", "Spain", "Spain"),
  stringsAsFactors = FALSE
)
reviewOverlap(df, Design, Country)
reviewOverlap(df, Design, Country, studlabs = TRUE)

}
