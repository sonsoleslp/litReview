% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_upset.R
\name{reviewUpset}
\alias{reviewUpset}
\title{Create an UpSet Plot of Value Combinations}
\usage{
reviewUpset(
  data,
  col,
  sep = "\\r\\n",
  study_id = StudyID,
  base_size = 12,
  na.rm = TRUE,
  na_label = "Not reported",
  n_intersections = 15,
  sort_by = c("freq", "degree"),
  fill = "#7BB0D1"
)
}
\arguments{
\item{data}{A data frame with at least the columns named by \code{col} and
\code{study_id}.}

\item{col}{Multi-value column to analyze (quoted or unquoted).}

\item{sep}{Character. Separator for multi-value cells. Defaults to \code{"\\r\\n"}.}

\item{study_id}{Column containing study identifiers (quoted or unquoted).
One combination is formed per study. Defaults to \code{StudyID}.}

\item{base_size}{Numeric. Base font size in points. Defaults to \code{12}.}

\item{na.rm}{Logical. Drop missing values? Defaults to \code{TRUE}.}

\item{na_label}{Character. Label for missing values when \code{na.rm = FALSE}.
Defaults to \code{"Not reported"}.}

\item{n_intersections}{Integer. Maximum number of combinations (bars) to
display, keeping the largest. Defaults to \code{15}.}

\item{sort_by}{Character. Order bars by \code{"freq"} (intersection size,
default) or \code{"degree"} (number of values in the combination).}

\item{fill}{Character. Bar and matrix-dot color. Defaults to \code{"#7BB0D1"}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Visualizes how the values of a multi-value column co-occur across studies.
Each study contributes the \emph{set} of distinct values it reports; the plot
shows the size of each observed combination (intersection) as a bar, with a
dot matrix beneath indicating which values make up that combination. This
scales past the pairwise \code{\link[=reviewOverlap]{reviewOverlap()}} when three or more values can
co-occur. Returns a standard \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\details{
Requires the \pkg{ggupset} package.
}
\examples{
df <- data.frame(
  StudyID = paste0("S", 1:5),
  Outcome = c("Pain", "Pain\nFunction", "Function",
              "Pain\nFunction\nQoL", "QoL"),
  stringsAsFactors = FALSE
)
if (requireNamespace("ggupset", quietly = TRUE)) {
  reviewUpset(df, Outcome, sep = "\n")
}

}
