% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize_data}
\alias{summarize_data}
\title{Summarize Frequency of Values in a Column}
\usage{
summarize_data(
  data,
  col,
  sep = "\\r\\n",
  study_id = StudyID,
  na.rm = TRUE,
  na_label = "Not reported",
  na_in_percent = TRUE,
  na_last = FALSE
)
}
\arguments{
\item{data}{A data frame containing at least the columns specified by
\code{col} and \code{study_id}.}

\item{col}{Column to summarize (quoted or unquoted).}

\item{sep}{Character. The separator used to split multi-value cells.
Defaults to \code{"\\r\\n"}.}

\item{study_id}{Column containing study identifiers (quoted or unquoted).
Defaults to \code{StudyID}.}

\item{na.rm}{Logical. If \code{TRUE} (default), rows with missing values (\code{NA}
or empty strings) in \code{col} are dropped. If \code{FALSE}, they are kept and
labelled with \code{na_label}.}

\item{na_label}{Character. Label used for missing values when
\code{na.rm = FALSE}. Defaults to \code{"Not reported"}.}

\item{na_in_percent}{Logical. If \code{TRUE} (default), the denominator for
percentages is the total number of rows (including missing). If \code{FALSE},
only non-missing rows are counted, so non-missing categories sum to
\eqn{\approx 100\%}.}

\item{na_last}{Logical. If \code{TRUE}, the missing-value row (when
\code{na.rm = FALSE}) is placed at the bottom of the result regardless of
its frequency. Defaults to \code{FALSE} (sorted by frequency like all other
rows).}
}
\value{
A data frame with columns for the grouping variable, \code{Studies}
(comma-separated study IDs), \code{Frequency}, and \code{Percent} (numeric, 0--100).
}
\description{
Splits multi-value cells, counts frequencies, and computes percentages.
}
\examples{
df <- data.frame(
  StudyID = c("S1", "S2", "S3", "S4"),
  Design = c("RCT", "Cohort", NA, "RCT"),
  stringsAsFactors = FALSE
)

# Drop NAs (default)
summarize_data(df, Design)

# Keep NAs with a custom label
summarize_data(df, Design, na.rm = FALSE, na_label = "Missing")

# Percentages of non-missing only
summarize_data(df, Design, na_in_percent = FALSE)

}
