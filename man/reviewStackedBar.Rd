% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_stacked_bar.R
\name{reviewStackedBar}
\alias{reviewStackedBar}
\title{Create a Stacked / Grouped Bar Chart}
\usage{
reviewStackedBar(
  data,
  col,
  group,
  position = c("fill", "stack"),
  fill = PALETTE,
  width = 0.7,
  sep = "\\r\\n",
  study_id = StudyID,
  base_size = 12,
  na.rm = TRUE,
  na_label = "Not reported",
  na_last = FALSE,
  labels = TRUE
)
}
\arguments{
\item{data}{A data frame with at least the columns named by \code{col} and
\code{group}.}

\item{col}{Primary category (quoted or unquoted); one horizontal bar per
value.}

\item{group}{Grouping/splitting column (quoted or unquoted); mapped to the
bar fill.}

\item{position}{Character. \code{"fill"} (default) scales each bar to 100\% to
compare proportions; \code{"stack"} shows raw counts.}

\item{fill}{Character vector of fill colors for the groups. Defaults to
\link{PALETTE}.}

\item{width}{Numeric. Bar width (0--1). Defaults to \code{0.7}.}

\item{sep}{Character. Separator for multi-value cells. Defaults to \code{"\\r\\n"}.}

\item{study_id}{Column containing study identifiers (quoted or unquoted).
Defaults to \code{StudyID}. Present for API consistency; not currently used.}

\item{base_size}{Numeric. Base font size in points. Defaults to \code{12}.}

\item{na.rm}{Logical. Drop missing values? Defaults to \code{TRUE}.}

\item{na_label}{Character. Label for missing values when \code{na.rm = FALSE}.
Defaults to \code{"Not reported"}.}

\item{na_last}{Logical. If \code{TRUE} (and \code{na.rm = FALSE}), place the missing
category last — at the bottom of the axis and the end of each bar/legend.
Defaults to \code{FALSE}.}

\item{labels}{Logical. Draw the count (or percentage) inside each segment?
Defaults to \code{TRUE}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Cross-tabulates a primary category (\code{col}) against a grouping variable
(\code{group}) and displays the result as horizontal stacked bars. Use
\code{position = "fill"} for within-category proportions (each bar sums to
100\%) or \code{position = "stack"} for raw counts. Both columns may contain
multi-value cells, which are split before counting. Returns a standard
\link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\examples{
df <- data.frame(
  StudyID = paste0("S", 1:6),
  Design  = c("RCT", "Cohort", "RCT", "Cohort", "RCT", "Cohort"),
  Risk    = c("Low", "High", "Low", "Moderate", "High", "Low"),
  stringsAsFactors = FALSE
)
reviewStackedBar(df, Design, Risk)
reviewStackedBar(df, Design, Risk, position = "stack")

}
