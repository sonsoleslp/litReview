% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_alluvial.R
\name{reviewAlluvial}
\alias{reviewAlluvial}
\title{Create an Alluvial (Sankey) Plot}
\usage{
reviewAlluvial(
  data,
  cols,
  sep = "\\r\\n",
  study_id = StudyID,
  colors = PALETTE,
  base_size = 12,
  na.rm = TRUE,
  na_label = "Not reported",
  labels = c("none", "prop", "count", "both"),
  flow_labels = FALSE,
  flow_alpha = 0.25,
  stratum_width = 0.5,
  axis_labels = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Character vector of column names to use as axes (left to right).}

\item{sep}{Character. Separator for multi-value cells. Defaults to \code{"\\r\\n"}.
When a column contains multi-value cells, each combination generates a
separate alluvium.}

\item{study_id}{Column containing study identifiers (quoted or unquoted).
Defaults to \code{StudyID}.}

\item{colors}{Character vector. Fill colors for strata, cycled or matched
by name. Defaults to \link{PALETTE}.}

\item{base_size}{Numeric. Base font size in points. Defaults to \code{12}.}

\item{na.rm}{Logical. Drop rows with missing values? Defaults to \code{TRUE}.}

\item{na_label}{Character. Label for missing values when \code{na.rm = FALSE}.
Defaults to \code{"Not reported"}.}

\item{labels}{Character. What to show on each stratum. One of \code{"none"}
(default), \code{"prop"} (proportion within axis), \code{"count"}, or \code{"both"}
(count and proportion).}

\item{flow_labels}{Logical. If \code{TRUE}, show counts on the flows between
strata. Defaults to \code{FALSE}.}

\item{flow_alpha}{Numeric. Transparency of flows (0--1). Defaults to \code{0.25}.}

\item{stratum_width}{Numeric. Width of stratum bars. Defaults to \code{0.5}.}

\item{axis_labels}{Character vector of custom axis labels (same length as
\code{cols}), or \code{NULL} to use the column names. Defaults to \code{NULL}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Shows co-occurrence and flow between categories across multiple columns.
Each study traces a path through the strata. Requires the
\pkg{ggalluvial} package.
}
\examples{
\dontrun{
df <- data.frame(
  StudyID  = paste0("S", 1:6),
  Design   = c("RCT", "Cohort", "RCT", "RCT", "Cohort", "RCT"),
  Quality  = c("High", "Low", "High", "Low", "Low", "High"),
  Outcome  = c("Positive", "Negative", "Positive", "Negative",
               "Positive", "Positive"),
  stringsAsFactors = FALSE
)
reviewAlluvial(df, c("Design", "Quality", "Outcome"))
}

}
