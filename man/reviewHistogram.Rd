% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_histogram.R
\name{reviewHistogram}
\alias{reviewHistogram}
\title{Create a Numeric Histogram}
\usage{
reviewHistogram(
  data,
  col,
  fill_by = NULL,
  bins = 30,
  binwidth = NULL,
  fill = "#7BB0D1",
  colors = PALETTE,
  sep = "\\r\\n",
  base_size = 12,
  na.rm = TRUE,
  na_label = "Not reported"
)
}
\arguments{
\item{data}{A data frame containing at least the column named by \code{col}.}

\item{col}{Numeric column to bin (quoted or unquoted).}

\item{fill_by}{Optional categorical column to stack the bars by (quoted or
unquoted). If \code{NULL} (default), bars are drawn in a single color.}

\item{bins}{Integer. Number of bins. Passed to
\code{\link[ggplot2:geom_histogram]{ggplot2::geom_histogram()}}. Ignored when \code{binwidth} is set.
Defaults to \code{30}.}

\item{binwidth}{Numeric. Optional bin width. Overrides \code{bins} when set.
Defaults to \code{NULL}.}

\item{fill}{Character. Bar fill color when \code{fill_by} is \code{NULL}. Defaults to
\code{"#7BB0D1"}.}

\item{colors}{Character vector. Fill colors cycled across categories when
\code{fill_by} is set. Defaults to \link{PALETTE}.}

\item{sep}{Character. Separator for multi-value cells in \code{fill_by}.
Defaults to \code{"\\r\\n"}.}

\item{base_size}{Numeric. Base font size in points; controls proportional
scaling of all text and elements. Defaults to \code{12}.}

\item{na.rm}{Logical. Drop rows with missing values in \code{col} (and, when
set, in \code{fill_by})? Defaults to \code{TRUE}.}

\item{na_label}{Character. Label for missing values in \code{fill_by} when
\code{na.rm = FALSE}. Defaults to \code{"Not reported"}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Bins the values of a numeric column and plots the frequency of each bin.
Optionally stacks bars by a second categorical column. Returns a
\link[ggplot2:ggplot]{ggplot2::ggplot} object that can be customized with \code{+}.
}
\examples{
df <- data.frame(
  StudyID     = paste0("S", 1:20),
  SampleSize  = c(30, 45, 60, 22, 88, 120, 35, 51, 74, 66,
                  40, 95, 110, 28, 72, 58, 41, 33, 80, 105),
  Design      = rep(c("RCT", "Cohort", "Case-control", "Other"), 5)
)
reviewHistogram(df, SampleSize, bins = 8)
reviewHistogram(df, SampleSize, fill_by = Design, bins = 8)

}
