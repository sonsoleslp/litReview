% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_matrix.R
\name{reviewMatrix}
\alias{reviewMatrix}
\title{Create a Study-by-Criteria Coding Matrix}
\usage{
reviewMatrix(
  data,
  cols,
  color_by = NULL,
  study_id = StudyID,
  levels = NULL,
  colors = PALETTE,
  show_counts = TRUE,
  base_size = 12,
  label_wrap = 20,
  empty_fill = "#FCFCE6",
  tile_color = "white",
  na.rm = TRUE,
  na_label = "Not reported"
)
}
\arguments{
\item{data}{A data frame with one row per study.}

\item{cols}{Character vector of criterion column names, in the order they
should appear on the x-axis. Each cell holds the code shown in the tile,
or \code{NA}/\code{""} for no tile.}

\item{color_by}{Optional column name (character) giving each study's category,
mapped to the tile fill (e.g. \code{"DocumentType"}). \code{NULL} (default) fills all
tiles with a single color.}

\item{study_id}{Column with the study labels for the y-axis (quoted or
unquoted). Defaults to \code{StudyID}.}

\item{levels}{Optional named character vector mapping cell codes to
descriptions for the "Level" legend, e.g.
\code{c(O = "Operationalized", D = "Discussed", M = "Mention")}. The names fix
the legend order. \code{NULL} (default) labels the legend with the codes
themselves.}

\item{colors}{Character vector of fill colors for the \code{color_by} categories.
Defaults to \link{PALETTE}.}

\item{show_counts}{Logical. Append \code{" (N=k)"} to each column header, where
\code{k} is the number of studies addressing that criterion. Defaults to \code{TRUE}.}

\item{base_size}{Numeric. Base font size in points. Defaults to \code{12}.}

\item{label_wrap}{Integer. Wrap axis labels longer than this many characters.
\code{NULL}/\code{Inf} disables. Defaults to \code{20}.}

\item{empty_fill}{Character. Fill for the background grid behind empty cells.
Defaults to \code{"#FCFCE6"}.}

\item{tile_color}{Character. Border color between tiles. Defaults to
\code{"white"}.}

\item{na.rm}{Logical. If \code{TRUE} (default), cells with a missing/empty value
are left blank (the study did not address that criterion). If \code{FALSE},
they are drawn as an explicit tile labelled with \code{na_label}.}

\item{na_label}{Character. Code shown in unaddressed cells when
\code{na.rm = FALSE}. Defaults to \code{"Not reported"}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws an evidence / coding matrix: one row per study, one column per
criterion, and a tile wherever a study addresses a criterion. The tile
\strong{fill} encodes an optional per-study attribute (e.g. document type) and the
\strong{letter} inside each tile is the cell's own value (e.g. a coding level such
as \code{O}/\code{D}/\code{M}). Column headers can carry the number of studies addressing
each criterion. Returns a standard \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\details{
The input is one row per study (wide format): a study-id column, an optional
grouping column for the fill, and one column per criterion holding the cell
code (or \code{NA}/empty where the study does not address that criterion).
}
\examples{
studies_wide <- data.frame(
  StudyID  = c("Tam 2024", "Schiff 2021", "Reddy 2023", "Yu 2023"),
  Type     = c("Journal", "Journal", "Journal", "Conference"),
  Accuracy = c("O", "D", "D", "M"),
  Equity   = c(NA, "D", "D", "O"),
  Ethics   = c("O", "D", "D", "O"),
  stringsAsFactors = FALSE
)
reviewMatrix(studies_wide, c("Accuracy", "Equity", "Ethics"),
             color_by = "Type",
             levels = c(O = "Operationalized", D = "Discussed", M = "Mention"))

}
