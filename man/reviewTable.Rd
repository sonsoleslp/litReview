% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_table.R
\name{reviewTable}
\alias{reviewTable}
\title{Create a Summary Table}
\usage{
reviewTable(
  data,
  col,
  sep = "\\r\\n",
  study_id = StudyID,
  latex = FALSE,
  cite = FALSE,
  na.rm = TRUE,
  na_label = "Not reported",
  na_in_percent = TRUE,
  na_last = FALSE
)
}
\arguments{
\item{data}{A data frame with at least a study ID column and the column
named by \code{col}.}

\item{col}{Column to summarize (quoted or unquoted).}

\item{sep}{Character. Separator for multi-value cells. Defaults to \code{"\\r\\n"}.}

\item{study_id}{Column containing study identifiers (quoted or unquoted).
Defaults to \code{StudyID}.}

\item{latex}{Logical. If \code{TRUE}, returns the table as LaTeX code via
\code{\link[gt:as_latex]{gt::as_latex()}} instead of a \code{\link[gt:gt]{gt::gt()}} object. Defaults to \code{FALSE}.}

\item{cite}{Logical. If \code{TRUE}, treats values in \code{study_id} as BibTeX
reference keys and wraps the per-row study list in \verb{\\\\cite\{...\}}.
Defaults to \code{FALSE}.}

\item{na.rm}{Logical. Drop missing values? Defaults to \code{TRUE}.}

\item{na_label}{Character. Label for missing values when \code{na.rm = FALSE}.
Defaults to \code{"Not reported"}.}

\item{na_in_percent}{Logical. Include missing rows in the percentage
denominator? Defaults to \code{TRUE}.}

\item{na_last}{Logical. If \code{TRUE}, place the missing-value category last
regardless of its frequency. Defaults to \code{FALSE}.}
}
\value{
A \code{\link[gt:gt]{gt::gt()}} table, or a LaTeX character string if \code{latex = TRUE}.
The LaTeX output prints cleanly in the console, and renders automatically
in knitr/R Markdown chunks with \code{results = "asis"}.
}
\description{
Summarizes a column from literature review data and returns a formatted
\code{\link[gt:gt]{gt::gt()}} table showing each category, its associated study IDs,
frequency, and percentage.
}
\examples{
df <- data.frame(
  StudyID = c("S1", "S2", "S3", "S4"),
  Design = c("RCT", "Cohort", "RCT", "Case-control"),
  stringsAsFactors = FALSE
)
reviewTable(df, Design)
reviewTable(df, Design, latex = TRUE, cite = TRUE)

}
