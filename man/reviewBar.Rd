% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_bar.R
\name{reviewBar}
\alias{reviewBar}
\title{Create a Summary Bar Chart}
\usage{
reviewBar(
  data,
  col,
  fill = "#7BB0D1",
  width = 0.6,
  sep = "\\r\\n",
  studlabs = FALSE,
  study_id = StudyID,
  label_space = 1.6,
  base_size = 12,
  na.rm = TRUE,
  na_label = "Not reported",
  na_in_percent = TRUE,
  na_last = FALSE
)
}
\arguments{
\item{data}{A data frame with at least a study ID column and the column
named by \code{col}.}

\item{col}{Column to visualize (quoted or unquoted).}

\item{fill}{Character. Bar fill color. Defaults to \code{"#7BB0D1"}.}

\item{width}{Numeric. Bar width (0--1). Defaults to \code{0.5}.}

\item{sep}{Character. Separator for multi-value cells. Defaults to \code{"\\r\\n"}.}

\item{studlabs}{Logical. If \code{TRUE}, draws study ID labels on bars using
\code{\link[ggfittext:geom_fit_text]{ggfittext::geom_bar_text()}}. Defaults to \code{FALSE}.}

\item{study_id}{Column containing study identifiers (quoted or unquoted).
Defaults to \code{StudyID}.}

\item{label_space}{Numeric. Multiplier for x-axis headroom to fit labels.
Defaults to \code{1.6}.}

\item{base_size}{Numeric. Base font size in points; controls proportional
scaling of all text and elements. Defaults to \code{12}.}

\item{na.rm}{Logical. Drop missing values? Defaults to \code{TRUE}.}

\item{na_label}{Character. Label for missing values when \code{na.rm = FALSE}.
Defaults to \code{"Not reported"}.}

\item{na_in_percent}{Logical. Include missing rows in the percentage
denominator? Defaults to \code{TRUE}.}

\item{na_last}{Logical. If \code{TRUE}, place the missing-value category last
regardless of its frequency. Defaults to \code{FALSE}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Summarizes a column from literature review data and produces a horizontal
bar chart with frequency and percentage labels. Returns a standard
\link[ggplot2:ggplot]{ggplot2::ggplot} object that can be customized with \code{+}.
}
\examples{
df <- data.frame(
  StudyID = c("S1", "S2", "S3", "S4"),
  Design = c("RCT", "Cohort", "RCT", "Case-control"),
  stringsAsFactors = FALSE
)
reviewBar(df, Design)
reviewBar(df, Design, fill = "#59a14f") + ggplot2::labs(title = "Designs")

}
