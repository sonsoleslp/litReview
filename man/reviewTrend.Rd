% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_trend.R
\name{reviewTrend}
\alias{reviewTrend}
\title{Create a Year-Trend Bar Chart}
\usage{
reviewTrend(
  data,
  col,
  year_col = Year,
  sep = "\\r\\n",
  colors = PALETTE,
  base_size = 12,
  na.rm = TRUE,
  na_label = "Not reported",
  na_last = FALSE,
  labels = c("none", "count", "percent", "both", "studies"),
  study_id = StudyID
)
}
\arguments{
\item{data}{A data frame with at least \code{StudyID} (or the column set by
\code{study_id}), the column named by \code{col}, and a year column.}

\item{col}{Column to visualize (quoted or unquoted).}

\item{year_col}{Year column (quoted or unquoted). Defaults to \code{Year}.}

\item{sep}{Character. Separator for multi-value cells. Defaults to \code{"\\r\\n"}.}

\item{colors}{Character vector. Fill colors cycled across categories.
Defaults to \link{PALETTE}.}

\item{base_size}{Numeric. Base font size in points. Defaults to \code{12}.}

\item{na.rm}{Logical. Drop missing values? Defaults to \code{TRUE}.}

\item{na_label}{Character. Label for missing values when \code{na.rm = FALSE}.
Defaults to \code{"Not reported"}.}

\item{na_last}{Logical. If \code{TRUE} (and \code{na.rm = FALSE}), place the missing
category last in the stack and legend. Defaults to \code{FALSE}.}

\item{labels}{Character. What to display on each bar segment. One of
\code{"none"} (default), \code{"count"}, \code{"percent"} (within-year),
\code{"both"} (count and percent), or \code{"studies"} (comma-separated study IDs).}

\item{study_id}{Column containing study identifiers (quoted or unquoted).
Used when \code{labels = "studies"}. Defaults to \code{StudyID}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Shows how the values in a column distribute across publication years as a
stacked bar chart. Returns a \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\examples{
df <- data.frame(
  StudyID = c("S1", "S2", "S3", "S4"),
  Year    = c(2020, 2021, 2021, 2022),
  Design  = c("RCT", "Cohort", "RCT", "Case-control"),
  stringsAsFactors = FALSE
)
reviewTrend(df, Design)
reviewTrend(df, Design, labels = "count")
reviewTrend(df, Design, labels = "percent")
reviewTrend(df, Design, labels = "studies")

}
