% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_treemap.R
\name{reviewTreemap}
\alias{reviewTreemap}
\title{Create a Treemap}
\usage{
reviewTreemap(
  data,
  col,
  color_by = NULL,
  sep = "\\r\\n",
  colors = PALETTE,
  base_size = 12,
  na.rm = TRUE,
  na_label = "Not reported",
  study_id = StudyID,
  studlabs = FALSE,
  border_col = "white"
)
}
\arguments{
\item{data}{A data frame.}

\item{col}{Column whose values define the rectangles (quoted or unquoted).}

\item{color_by}{Optional second column (quoted or unquoted) used to fill
the rectangles. When supplied the treemap is grouped hierarchically by
\code{color_by}, with \code{col} nested inside. When \code{NULL} (default), rectangles
are colored by \code{col} itself.}

\item{sep}{Character. Separator for multi-value cells. Defaults to \code{"\\r\\n"}.}

\item{colors}{Character vector. Fill colors, cycled or matched by name.
Defaults to \link{PALETTE}.}

\item{base_size}{Numeric. Base font size in points. Defaults to \code{12}.}

\item{na.rm}{Logical. Drop missing values? Defaults to \code{TRUE}.}

\item{na_label}{Character. Label for missing values when \code{na.rm = FALSE}.
Defaults to \code{"Not reported"}.}

\item{study_id}{Column containing study identifiers (quoted or unquoted).
Defaults to \code{StudyID}.}

\item{studlabs}{Logical. If \code{TRUE}, show study IDs inside each rectangle.
Defaults to \code{FALSE}.}

\item{border_col}{Character. Color of rectangle borders. Defaults to
\code{"white"}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Displays category frequencies as nested rectangles whose area is
proportional to the count. Optionally color by a second column for
a hierarchical view. Requires the \pkg{treemapify} package.
}
\examples{
df <- data.frame(
  StudyID = paste0("S", 1:8),
  Design  = c("RCT", "Cohort", "RCT", "RCT", "Cohort", "RCT", "Cohort", "RCT"),
  Quality = c("High", "Low", "High", "Low", "Low", "High", "Low", "High"),
  stringsAsFactors = FALSE
)
if (requireNamespace("treemapify", quietly = TRUE)) {
  reviewTreemap(df, Design)
  reviewTreemap(df, Design, color_by = Quality)
}

}
